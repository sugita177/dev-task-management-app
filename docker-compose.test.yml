services:
  db_test:
    image: postgres:15-alpine
    container_name: dev_task_db_test
    environment:
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: dev_task_db_test
      TZ: Asia/Tokyo
    ports:
      - "5433:5432"
